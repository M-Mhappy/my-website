name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Package site with tar
        # 输出写到 /tmp，避免 tar 在打包目录内写入导致 "file changed as we read it"（exit 1）
        run: tar czf /tmp/site-deploy.tar.gz --exclude='.git' --exclude='.github' -C "$GITHUB_WORKSPACE" .

      - name: Copy tarball to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 47.103.197.64
          username: admin
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "/tmp/site-deploy.tar.gz"
          target: "/tmp/"
          strip_components: 0
          overwrite: true

      - name: Extract on server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 47.103.197.64
          username: admin
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd /var/www/my-website
            find . -maxdepth 1 ! -name '.' ! -name 'my-website-deploy.zip' -exec rm -rf {} +
            tar xzf /tmp/site-deploy.tar.gz -C /var/www/my-website/
            rm -f /tmp/site-deploy.tar.gz
            echo "✅ Deployed at $(date)"
            curl -sf http://localhost/ > /dev/null && echo "✅ Site accessible" || echo "❌ Site check failed"