# 🌊 群岛个人网站 — 开发文档

> 项目代号：**Archipelago**
> 风格：2.5D 等轴 · 水彩卡通 · 动森质感 · 几何几何但非极简
> 技术栈：Three.js（主渲） + 原生 HTML/CSS/JS（UI 层）

---

## 目录

1. [项目概述](#1-项目概述)
2. [视觉设计语言](#2-视觉设计语言)
3. [整体架构](#3-整体架构)
4. [场景构成详解](#4-场景构成详解)
5. [交互流程](#5-交互流程)
6. [八岛设计规范](#6-八岛设计规范)
7. [UI 覆盖层](#7-ui-覆盖层)
8. [响应式策略](#8-响应式策略)
9. [性能与质量基线](#9-性能与质量基线)
10. [实现路线图](#10-实现路线图)
11. [附录：技术参考](#11-附录技术参考)

---

## 1. 项目概述

### 1.1 产品定位

个人品牌官网，以「群岛」隐喻人生的不同侧面。访客在一片柔和动态的海面上看到八座风格各异的岛屿，每座岛屿代表一个兴趣/经历主题，点击即可进入详情卡片。整体传递「探索」、「自然」、「多元」、「温暖」的品牌感受。

### 1.2 核心体验目标

| 维度 | 目标 |
|------|------|
| **第一印象** | 进入页面即被柔和海面吸引，视野开阔，有探索欲 |
| **探索感** | 群岛自然散布，大小远近不一，像真实的航拍群岛 |
| **细节惊喜** | 每座岛屿有独特的微动画（音符飘动、球场灯光闪烁等） |
| **交互流畅** | 点击转场无卡顿，卡片弹出流畅，背景模糊过渡自然 |
| **品牌传递** | 通过设计传达「有趣的灵魂有多个维度」 |

---

## 2. 视觉设计语言

### 2.1 色彩系统

以**柔和、低饱和、高明度**为总原则，参考动森/吉卜力的色彩哲学。

```
🌊 海洋主色（背景 / 动态水面）
  • 浅水区：#7EC8E3 → #A8D8EA（渐变）
  • 深水区：#5B9BD5 → #4A90D9
  • 浪花/波光：#FFFFFF（10% 透明度）

🏝️ 岛屿基座
  • 岩石：#C4B59A（暖灰褐）
  • 草地：#8BC34A → #A5D6A7（柔绿渐变）
  • 沙滩：#F5E6CA

🎨 八岛主题色（每个岛屿的主色调）
  • 成长轨迹（地图）：#F9A825（金盏黄）
  • 个人经历（树）：#66BB6A（森林绿）
  • 旅行：  　  #4FC3F7（天空蓝）
  • 文学：  　  #AB47BC（丁香紫）
  • 音乐：  　  #EC407A（樱花粉）
  • 影视：  　  #FF7043（落日橙）
  • 足球：  　  #26A69A（草绿）
  • 美食：  　  #FFCA28（蛋黄色）

🖼️ UI 覆盖层
  • 卡片背景：#FFFDF7（米白，水彩纸纹理）
  • 主文字：  #3E3A39（暖黑）
  • 辅助文字：#8C8984
  • 强调色：  各岛屿主题色
  • 毛玻璃：  backdrop-filter: blur(24px) saturate(0.8)
```

### 2.2 字体系统

| 层级 | 字体 | 备注 |
|------|------|------|
| 标题/Logo | `"ZCOOL KuaiLe", "Noto Sans SC", cursive` | 圆润卡通感 |
| 正文 | `"Noto Sans SC", "PingFang SC", sans-serif` | 高可读性 |
| 英文装饰 | `"Fredoka One", "Nunito", sans-serif` | 圆体 |

### 2.3 水彩效果实现策略

不在 Three.js 后处理中做复杂的 NPR 渲染，而是：

1. **岛屿模型**: 几何体 + 低多边形风格，边缘用 `MeshToonMaterial` 或自定义着色器加柔和边缘光
2. **纹理叠加**: 程序化噪声纹理叠加到颜色通道，产生水彩纸的轻微颗粒感（Canvas 生成噪声图 → 作为纹理 uniform）
3. **CSS 后处理叠加层**: 在 Three.js Canvas 上方覆盖一层 `mix-blend-mode: multiply` 的水彩纸纹理 PNG（透明背景），统一叠加画面质感
4. **边缘柔化**: 岛屿边缘用半透明渐变 mesh 做"水彩晕开"效果

> 优点：Three.js 负担小，渲染性能好，同时获得统一的水彩质感。

---

## 3. 整体架构

### 3.1 技术选型

```
┌─────────────────────────────────────────┐
│            HTML 结构层                    │
│  <canvas#three-canvas>  +  <div#ui-overlay>│
├─────────────────────────────────────────┤
│           Three.js 渲染层                │
│  • 场景 / 相机 / 渲染器                  │
│  • 海面（动态顶点波浪）                   │
│  • 8 座岛屿（Group 容器）                │
│  • 光照（环境光 + 方向光 + 半球光）       │
│  • 环境粒子（飞鸟/云/光点）              │
│  • 后处理（简单色调 + 泛光）             │
├─────────────────────────────────────────┤
│            UI 覆盖层                      │
│  • 左上：个人资料卡片                     │
│  • 岛屿 hover 提示（名称标签）            │
│  • 弹出卡片（Modal）                     │
│  • 加载 / 过渡动画控制器                 │
├─────────────────────────────────────────┤
│          交互控制层                       │
│  • Raycaster 点击检测                     │
│  • 场景淡出 → 卡片淡入                   │
│  • 卡片关闭 → 场景淡入                   │
│  • 响应式适配                             │
└─────────────────────────────────────────┘
```

### 3.2 依赖清单

| 依赖 | 用途 | 版本建议 |
|------|------|----------|
| Three.js | 3D 场景核心 | r160+ |
| OrbitControls | 仅调试模式 | r160+ |
| GSAP | UI 过渡动画（场景淡出/卡片动画） | 3.12+ |
| 无其他第三方库 | 保持轻量，避免臃肿 | — |

> **CDN 加载方案**（推荐开发阶段使用）：
> - Three.js: `https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js`
> - GSAP: `https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js`
>
> **生产建议**：使用 ES Module 方式 import，配合构建工具 tree-shaking。

### 3.3 文件结构

```
/
├── index.html              # 入口页面
├── assets/
│   ├── textures/
│   │   ├── water-noise.png         # 水面法线/噪声纹理（程序生成或手绘）
│   │   ├── watercolor-paper.png    # 水彩纸纹理（叠加质感）
│   │   └── island-grass.png        # 岛屿草地纹理（可选）
│   ├── models/
│   │   └── (空，使用 Three.js 几何体组合)  # 说明：所有岛屿用代码构建
│   └── fonts/
│       └── (Google Fonts 在线加载)
├── css/
│   ├── reset.css           # 基础重置
│   ├── main.css            # 全局样式 + UI 覆盖层样式
│   └── modal.css           # 弹出卡片样式
├── js/
│   ├── main.js             # 入口：初始化场景、启动循环
│   ├── scene.js            # Three.js 场景/相机/渲染器配置
│   ├── water.js            # 动态海面（顶点波浪）
│   ├── islands/
│   │   ├── IslandBase.js   # 岛屿基座（岩石+草地）工厂
│   │   ├── growth.js       # 成长轨迹 → 地图岛屿
│   │   ├── experience.js   # 个人经历 → 树岛屿
│   │   ├── travel.js       # 旅行岛屿
│   │   ├── literature.js   # 文学 → 书岛屿
│   │   ├── music.js        # 音乐 → 剧院岛屿
│   │   ├── film.js         # 影视 → 电影院岛屿
│   │   ├── football.js     # 足球 → 球场岛屿
│   │   └── food.js         # 美食 → 餐厅岛屿
│   ├── interaction.js      # Raycaster 点击检测 + hover 效果
│   ├── animations.js       # 岛屿微动画（浮动、旋转）
│   ├── particles.js        # 环境粒子（光点、飞鸟）
│   └── ui/
│       ├── profileCard.js  # 左上角个人资料卡片
│       ├── islandLabel.js  # hover 标签
│       └── modal.js        # 弹出卡片管理
├── docs/
│   └── (设计稿 / 参考图)
└── README.md
```

---

## 4. 场景构成详解

### 4.1 相机配置

```
类型: PerspectiveCamera
FOV:    45°
位置:   (0, 18, 22)
目标:   (0, 0, 0)
近裁面:  0.1
远裁面: 100

固定视角：不做 OrbitControls（发布模式）
调试模式：可启用 OrbitControls 方便摆岛
```

### 4.2 光照系统

```
环境光 AmbientLight
  • color: #ffffff
  • intensity: 0.5

半球光 HemisphereLight
  • sky: #87CEEB（天蓝）
  • ground: #C8B89A（暖沙色）
  • intensity: 0.8

方向光 DirectionalLight
  • color: #FFF5E6（暖白）
  • position: (10, 20, 5)
  • intensity: 1.2
  • castShadow: true（岛屿投射柔和阴影到水面）

可选：主光上加柔和阴影贴图（soft shadow）
```

### 4.3 海面实现

#### 4.3.1 几何结构

```
平面几何体，细分 64×64 网格
尺寸：约 40×40 单位（覆盖整个视野）

顶点动画（每帧更新 position.y）：
  使用正弦波叠加：
    y = A1·sin(ω1·x + φ1·t) + 
        A2·sin(ω2·z + φ2·t) + 
        A3·sin(ω3·(x+z) + φ3·t)

  参数建议：
    A1 = 0.08, ω1 = 0.8, φ1 = 0.6
    A2 = 0.05, ω2 = 0.5, φ2 = 0.8
    A3 = 0.03, ω3 = 0.3, φ3 = 1.2

  频率较低 → 大浪柔和，像风吹过的湖面
```

#### 4.3.2 材质

```
ShaderMaterial 或自定义：
  • 颜色：浅水区 #7EC8E3 → 深水区 #4A90D9（根据高度或径向渐变）
  • 透明：0.85
  • 光泽：微弱高光（Blinn-Phong 近似）
  • 法线：程序噪声纹理偏移（水面波纹细节）
  • 边缘：与天空交接处渐变为透明

可选效果：
  • 波光粼粼：在顶点位置不规律闪光的粒子化高光点
    （用 points 系统或片元着色器加噪声门控的高光）
```

### 4.4 岛屿通用结构（IslandBase）

每个岛屿是一个 `THREE.Group`，包含：

```
Group(island)
├── Mesh(岩石基座)    — 圆角锥台/不规则多边形柱体，暖灰褐色
├── Mesh(草地层)      — 稍大的扁圆柱体盖在岩石上，绿色渐变
├── Mesh(沙滩边缘)    — 可选，部分岛屿有浅色沙滩过渡到水
├── Group(主题建筑)    — 各岛屿独有的主题几何组合（见第6节）
└── PointLight / 点缀  — 可选，夜晚模式或氛围光
```

#### 基座参数

```
岩石基座：
  形状：圆柱挤压 → 顶部顶点轻微随机偏移（自然感）
  半径：2.0 ~ 4.0（根据岛屿大小）
  高度：1.0 ~ 1.5
  颜色：C4B59A
  材质：MeshStandardMaterial, roughness: 0.9

草地层：
  形状：扁圆柱，略大于岩石基座半径（0.2 单位悬挑）
  半径：岩石半径 + 0.2
  高度：0.15
  颜色：8BC34A → A5D6A7（径向渐变）
  材质：MeshStandardMaterial, roughness: 0.8

沙滩边缘（部分岛屿）：
  形状：扁圆环，草地外围
  颜色：F5E6CA
```

### 4.5 背景与氛围元素

#### 4.5.1 天空

```
纯色渐变或简单天空球：
  上方：#B3E5FC（浅蓝）
  地平线：#E8F5E9 → #FFF8E1（暖白）
  下方（水面以下）：不渲染（海面已覆盖）
  
或用 Three.js 的 Fog / FogExp2 制造深度感
```

#### 4.5.2 云朵（装饰性）

```
3-5 朵半透明扁平球体组合，缓慢漂移
  • 位置：高处随机散布（y: 12-18）
  • 材质：MeshBasicMaterial, transparent, 白色 0.3-0.5 透明度
  • 动画：沿 x 轴缓慢平移（speed: 0.02-0.05），循环
  • 风格：卡通扁平云（3-4个球体拼成）
```

#### 4.5.3 环境光点粒子

```
Points 系统，约 100-200 个粒子
  • 分布：水面附近，随机位置
  • 大小：0.05-0.15
  • 颜色：#FFFFFF → #FFFDE7（暖白到浅黄）
  • 透明度：0.2-0.6，闪烁
  • 动画：缓慢上下浮动 + 大小缓动变化
  
营造「阳光洒在海面，光点跃动」的感觉
```

---

## 5. 交互流程

### 5.1 点击岛屿 → 弹出卡片

```
用户 hover 到岛屿
  └─ 岛屿轻微放大（scale 1.0 → 1.08）
  └─ 名称标签 UI 显示在岛屿上方
      ├─ 标签样式：圆角 pill，毛玻璃背景
      └─ 内容：岛屿中文名 + 小图标（emoji）

用户点击岛屿
  └─ 触发 GSAP 时间线：
      1. 镜头轻微 push in（相机向前移动 1-2 单位）（0.3s）
      2. 画面亮度降低（暗色叠加层 opacity 0→1）（0.4s）
      3. 海面和岛屿渐渐透明（opacity 1→0）（0.5s）
      4. 暗色叠加层全程覆盖
  └─ 暗色层上方出现毛玻璃卡片（横向居中，纵向居中）
      ├─ 卡片从下方滑入 + 透明度 0→1（0.4s, ease: "back.out(1.4)"）
      ├─ 卡片背景：水彩纸纹理 + 对应岛屿主题色点缀
      ├─ 卡片宽度：桌面 65vw（最大 780px），移动 92vw
      ├─ 卡片高度：自适应内容，最大 85vh（溢出滚动）
      └─ 卡片关闭按钮：右上角 ✕ 圆形按钮
```

### 5.2 关闭卡片 → 返回主场景

```
用户点击关闭按钮 / 点击卡片外暗色区域
  └─ 触发 GSAP 时间线（反向）：
      1. 卡片向下滑出 + 透明度 1→0（0.3s）
      2. 暗色叠加层 opacity 1→0（0.4s）
      3. 海面和岛屿渐渐显回（opacity 0→1）（0.5s）
      4. 相机回到原位（0.3s）
  └─ 恢复场景交互
```

### 5.3 岛屿微动画（常驻）

每个岛屿持续播放以下动画，增强生命力：

```
浮动动画（Float Animation）：
  island.position.y = baseY + sin(time * speed) * amplitude
  • amplitude: 0.08-0.15（不同岛屿不同，更自然）
  • speed: 0.4-0.8（随机差异）

呼吸动画（Breathing Scale）：
  island.scale.x = island.scale.z = 1 + sin(time * 0.3) * 0.005
  （微乎其微的缩放，模拟"呼吸"）

旋转动画（仅部分岛屿的装饰物）：
  风车、音符等装饰物缓慢自转
```

### 5.4 加载过渡

```
页面加载时：
  1. 显示加载指示器（简单的 CSS 动画 + "正在建造群岛..." 文案）
  2. Three.js 场景初始化完毕 → 岛屿从水下依次浮出
     ├─ 每个岛屿从 y = -2 升到指定位置（GSAP，stagger: 0.15s）
     └─ 伴随轻微缩放动画（0.8 → 1.0）
  3. 海面逐渐显现（透明度 0→1）
  4. 加载指示器淡出
  5. 左上角个人卡片从左侧滑入
  
  ⏱ 总时长约 1.5-2s
```

---

## 6. 八岛设计规范

### 6.1 总览

| # | 岛屿 | 主题色 | 基座大小 | 核心几何形态 | 装饰/动效 |
|---|------|--------|----------|-------------|-----------|
| 1 | 成长轨迹 | #F9A825 | 2.5 | 卷轴地图 | 路线点闪烁 |
| 2 | 个人经历 | #66BB6A | 3.0 | 大树 | 树叶飘落 |
| 3 | 旅行 | #4FC3F7 | 2.2 | 热气球+地球 | 气球浮动 |
| 4 | 文学 | #AB47BC | 2.5 | 打开的书本 | 书页翻动 |
| 5 | 音乐 | #EC407A | 3.0 | 歌剧院 | 音符粒子 |
| 6 | 影视 | #FF7043 | 2.8 | 电影院 | 胶片旋转 |
| 7 | 足球 | #26A69A | 3.2 | 足球场 | 场灯闪烁 |
| 8 | 美食 | #FFCA28 | 2.5 | 餐厅+餐盘 | 炊烟动画 |

### 6.2 岛屿位置布局

自然散布，模拟真实群岛的随机感，同时保持视觉平衡：


### 6.3 各岛屿详细构建

#### 6.3.1 🗺️ 成长轨迹（Growth Track）— 地图岛

```
主题建筑：
  • 一张半展开的卷轴地图（用弯曲平面 + 平面几何）
  • 地图上标注 3-5 个发光的路径点（小球体）
  • 边界用棕色柱体模拟卷轴杆

装饰：
  • 路径点微微发光 pulsating（点光源或自发光材质）
  • 一条细线连接路径点（LineGeometry）
  •  compass 小标志（可选）

基座：岩石 + 草地，偏小
```

#### 6.3.2 🌳 个人经历（Experience）— 树岛

```
主题建筑：
  • 一棵风格化大树（树干：棕色圆柱/锥体，树冠：2-3 层球体叠成）
  • 树冠使用 MeshToonMaterial，绿色渐变
  • 树干底部有盘根错节的装饰（小圆柱弯曲排列）
  • 树下有小秋千或小木牌（可选细节）

装饰：
  • 3-5 片树叶从树冠飘落（粒子系统或单独 Mesh），循环
  • 树冠微微 sway（受"风"影响）

基座：最大之一，草地延伸到树干周围
```

#### 6.3.3 ✈️ 旅行（Travel）— 旅行岛

```
主题建筑：
  • 热气球（篮子 + 彩色条纹球体，用 SphereGeometry 拉伸）
  • 热气球用绳索（CylinderGeometry, radiusTop=0.02）连接到篮子
  • 旁边立一个小地球仪（球体 + 弧线环）

装饰：
  • 热气球持续上下浮动（浮动幅度 0.3，比基座浮动大）
  • 地球仪缓慢自转
  • 小旗子飘扬（PlaneGeometry 波浪变形）

基座：中等大小，沙滩边缘明显
```

#### 6.3.4 📚 文学（Literature）— 书岛

```
主题建筑：
  • 一本打开的书（两个倾斜平面作为书页 + 书脊 BoxGeometry）
  • 书页用半透明材质（0.85 透明度），微黄 #FFF8E1
  • 书页上有隐约的文字纹理（Canvas 生成微小线条纹理）
  • 一支羽毛笔斜倚在书脊处（Cylinder 弯曲 + Shape 羽毛）

装饰：
  • 书页轻轻翻动动画（page flip 循环，用顶点动画）
  • 飘出 2-3 个文字符号粒子（"文""字""📖" 形状的 Sprite）
  • 发光小点从书页中升起

基座：中等，带一点紫色调点缀
```

#### 6.3.5 🎵 音乐（Music）— 大剧院岛

```
主题建筑：
  • 风格化歌剧院/音乐厅
  • 主体：半圆形穹顶（SphereGeometry 半截）+ 主建筑 Box
  • 正面：罗马柱列（3-5 根圆柱） + 三角楣
  • 顶部：金色音乐符号装饰（♪ ♫ 3D 形状或用 Sprite）

装饰：
  • 音符粒子从剧院飘出，沿弧线上升消散（10-15 个）
  • 音符用 Sprite 或 ExtrudeGeometry 制作 ♪ 形状
  • 剧院窗户透出暖光（点光源 + 自发光矩形）
  • 穹顶有星星点点的灯光

基座：较大，优雅庄重
```

#### 6.3.6 🎬 影视（Film）— 电影院岛

```
主题建筑：
  • 经典老式电影院门面
  • 主体：高耸的 Box（电影院建筑）
  • 正面：大型屏幕（发光矩形，轻微闪烁模拟放映）
  • 入口：拱门 + 遮雨棚
  • 两侧：电影胶片条装饰（环状扭曲带）

装饰：
  • 胶片环缓慢旋转（TorusGeometry 扭曲成 Moebius 状，或环形排列的矩形条）
  • 放映光束：从建筑顶部射出的锥形半透明光柱
  • 闪烁的霓虹灯牌（"CINEMA" 文字 Sprite 或 Extrude）

基座：中大型
```

#### 6.3.7 ⚽ 足球（Football）— 足球场岛

```
主题建筑：
  • 从上方看的足球场（俯视感，但稍有 3D 厚度）
  • 椭圆草坪（绿色扁圆柱 / 椭圆几何体）
  • 草坪上的球场线（LineLoop 勾勒边线、中线、罚球区）
  • 四个角落的弧光灯柱（细圆柱 + 小球体发光）
  • 两个小球门（迷你 Box 组合）

装饰：
  • 一个足球（二十面体 IcosahedronGeometry）在球场中央
  • 足球缓慢自转
  • 灯柱发出光柱（Cylinder 半透明光锥）
  • 球场灯光在傍晚模式下闪烁（如果实现日夜）

基座：最大的岛屿之一，带大量草地
```

#### 6.3.8 🍽️ 美食（Food）— 餐厅岛

```
主题建筑：
  • 卡通餐厅建筑：坡屋顶小屋（Box + 三角棱柱屋顶）
  • 一个大餐盘在餐厅前方（扁圆柱体 + 食物装饰）
  • 烟囱伸出屋顶

装饰：
  • 炊烟从烟囱飘出（3-4 个半透明球体渐大上升，循环）
  • 餐盘上有微型食物（彩色小几何体：红 Sphere（番茄）、绿 Cylinder（蔬菜）等）
  • 餐厅窗户透出暖黄光
  • 小招牌（用 Sprite 或小型 Plane）

基座：中型，暖色调
```

---

## 7. UI 覆盖层

### 7.1 左上角个人资料卡片

```
位置：左上角，距边缘 24px
样式：
  • 毛玻璃背景：backdrop-filter: blur(16px)
  • 圆角：16px
  • padding: 16px 20px
  • 阴影：subtle box-shadow

内容：
  ┌─────────────────┐
  │  [头像]  姓名    │
  │          联系方式  │
  │          📧 ✉️   │
  └─────────────────┘

头像：80×80px 圆形，边框 2px 白色
姓名：18px, bold
联系方式：13px, 辅助色, 含 icon
```

### 7.2 岛屿 hover 标签

```
位置：岛屿上方 1.5 单位（Three.js 坐标 → 通过 vector3.project 转屏幕坐标）
显示条件：鼠标 hover 到岛屿 Raycaster 命中时
样式：
  • 圆角 pill：border-radius: 20px
  • padding: 6px 16px
  • 毛玻璃背景
  • 字体 14px
  • 带 emoji 图标

内容示例：「🎵 音乐」「⚽ 足球」
```

### 7.3 弹出卡片（Modal）

```
结构：
  ┌─────────────────────────────────────────────────┐
  │  ✕ (关闭按钮，右上)                              │
  │                                                   │
  │  岛屿主题图标 + 标题                              │
  │  ─────────────────────────────────               │
  │                                                   │
  │  内容区域（可滚动）                                │
  │  （具体内容由后续开发填充，目前留白）               │
  │                                                   │
  │                                                   │
  └─────────────────────────────────────────────────┘

样式：
  • 背景：#FFFDF7
  • 圆角：24px
  • 宽度：桌面 65vw（max 780px），移动 92vw
  • 最大高度：85vh，溢出时内部滚动（overflow-y: auto）
  • 顶部：岛屿主题色条纹（8px 高）
  • 关闭按钮：40×40px 圆形，毛玻璃，hover 变主题色
  • 出场动画：GSAP from y: 60px + opacity: 0 → y: 0 + opacity: 1
  • 时间：0.4s, ease: "back.out(1.4)"

外部暗色蒙层：
  • 全屏 fixed
  • 背景：rgba(0, 0, 0, 0.55)
  • backdrop-filter: blur(8px)（毛玻璃模糊场景残影）
  • z-index: 999
  • 点击蒙层 = 关闭卡片
```

---

## 8. 响应式策略

### 8.1 总体原则

**Desktop-first**，大屏是主战场，移动端做合理降级和适配

---

## 9. 性能与质量基线

### 9.1 性能目标

| 指标 | 目标 |
|------|------|
| 首屏加载时间 | ≤ 3s (3G 下 ≤ 5s) |
| 帧率（桌面） | 稳定 60fps |
| 帧率（手机） | ≥ 30fps |
| JS 包体积 | ≤ 500KB（未压缩） |
| 纹理总大小 | ≤ 2MB |

### 9.2 质量检查清单

```
□ 所有岛屿在 45° 视角下无遮挡
□ 海面动画流畅无卡顿
□ 点击岛屿准确命中（Raycaster 阈值合理）
□ 卡片弹出/关闭过渡顺滑
□ 手机端触摸交互正常
□ 无控制台报错
□ 字体正确加载
□ 所有纹理正确显示
□ 加载过渡完整
□ 响应式断点正确触发
```



